This app shows how to add an external dependency. 

We need to run:

```sh
make install
```

To install dependency before sharing. 

Please see `Makefile` for install recipe. Requirements are saved in requirements.txt